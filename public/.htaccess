RewriteEngine On

# Keep real files and directories untouched.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send only unknown client-side routes to the React entry point.
RewriteRule ^ index.html [L]
